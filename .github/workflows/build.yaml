name: CI

on:
  pull_request:
  push:
    branches:
      - main
  merge_group:

# A pull request only needs an answer about its newest commit. Superseded runs
# are cancelled so the queue reflects what is actually up for review. Pushes to
# main keep their runs — main's history is the record.
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

# The floor for every job. A job that needs more says so itself, so that reading
# one job tells you what that job can reach.
permissions:
  contents: read

jobs:
  # The whole pipeline: fmt, vet, golangci-lint and `go test -race`, as the root
  # Dagger module defines them and as `dagger call ci` runs them on a
  # contributor's machine. It is one call and it stays one — a step here that
  # reran any of those stages would be a second definition of the standard, and
  # the two would drift. See CONTRIBUTING.md and .dagger/main.go.
  #
  # Repo-specific verification lands in this workflow the same way: as another
  # function on the root module invoked by another `dagger call`, never as raw
  # Go steps beside it. That is why the example round-trip is the
  # `dagger call regeneration` step below rather than a handful of `go build`
  # steps here, and why the ubuntu/macos/windows matrix this file used to carry
  # is gone: Dagger runs Linux containers, so a matrix over runners buys nothing
  # once the pipeline is a container, and the Windows half of it has no target
  # left to check — avroc targets POSIX hosts, as docs/plugin/SPEC.md records.
  # The platforms that do still differ are covered inside the module, by the
  # regeneration stage, which runs on each of them.
  build:
    name: Build
    runs-on: ubuntu-latest
    permissions:
      # Reading the source is all this job does. No `packages: write`, because
      # nothing is pushed; no `id-token: write`, because nothing is attested.
      # The module does now produce an artifact — the IR FileDescriptorSet
      # below — but this job only builds it and throws it away; the grant that
      # publishes it lives on the job that publishes it, in release.yaml.
      contents: read
    # Every `dagger call` below publishes its trace to Dagger Cloud, and this is
    # the whole of the mechanism: the CLI uploads whenever a Cloud token is in
    # its environment. There is nothing to install and no flag to pass.
    #
    # It answers the questions this log cannot. Most of these calls fan out over
    # linux/amd64 and linux/arm64 and build containers inside containers, so a
    # red step here is a wall of interleaved output, and *which platform*,
    # *which stage inside the module* and *why it took eleven minutes* are
    # things you would otherwise read by eye. The trace already records all
    # three; before this it was recorded and thrown away.
    #
    # Deliberately not `dagger/dagger-for-github`. Its `version` input takes a
    # literal semver or `latest`, and both workflows here install the CLI at the
    # `engineVersion` read out of dagger.json specifically so that no Dagger
    # version is ever typed into a workflow file — the same argument as
    # .golangci.yml and the --platform matrix living in the module. The action
    # buys one environment variable and costs that.
    #
    # The scope is this job rather than the workflow, and the value is never
    # given to the module: it reaches the CLI and stops there. Nothing the
    # module builds sees it, because Dagger puts host environment inside a
    # container only where the module asks for it, and none of these functions
    # ask.
    #
    # This is Dagger's own telemetry. `OTEL_EXPORTER_OTLP_*` is a different
    # lever pointing at a different backend; setting one has nothing to do with
    # the other.
    #
    # One-time setup: sign in to Dagger Cloud, open the organisation's Settings
    # -> Tokens (https://dagger.cloud/<org>/settings?tab=Tokens), and add the
    # value as a repository secret named DAGGER_CLOUD_TOKEN. It is an ingest
    # credential for that organisation's traces and grants nothing in this
    # repository. DAGGER_CLOUD_TOKEN is the name the pinned engineVersion reads
    # — _EXPERIMENTAL_DAGGER_CLOUD_TOKEN is the name it replaced, and v0.21.8
    # does not look at it.
    env:
      DAGGER_CLOUD_TOKEN: ${{ secrets.DAGGER_CLOUD_TOKEN }}
    steps:
      # A shallow checkout on purpose. The module excludes .git from the source
      # directory it checks, because none of fmt, vet, lint or test reads git
      # metadata, so there is nothing here for the extra history to feed. That
      # changes with the archetype: a GoApp stamps binaries from the refs at
      # HEAD and needs real metadata, so `fetch-depth: 0` comes back in the same
      # change that drops .git from the module's ignore list.
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      # Pinned to the engineVersion the module itself declares, read out of
      # dagger.json rather than repeated here. The CLI provisions an engine of
      # its own version, so a number typed into this file could drift from the
      # module's and leave CI running the pipeline on an engine the module has
      # never been run against — which is the difference between this repository
      # and every other one that the module exists to prevent. It is the same
      # command CONTRIBUTING.md gives contributors, for the same reason.
      - name: Install the Dagger CLI
        run: |
          curl -fsSL https://dl.dagger.io/dagger/install.sh \
            | DAGGER_VERSION="$(jq -r .engineVersion dagger.json | tr -d v)" \
              BIN_DIR="$HOME/.local/bin" sh
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      # Degrade loudly, not silently, and only where somebody can act on it. A
      # run without the token still passes and simply publishes nothing — which
      # is the permanent state of a pull request from a fork, because GitHub
      # denies forks secrets by design and nothing on that side could fix it.
      # Warning there would put a defect on every outside contributor's first
      # pull request that is not one.
      #
      # Every other run — a branch in this repository, main, the merge queue —
      # is missing the token only because nobody has added the secret yet, and
      # what is lost is the trace of the run being read. So that case says so,
      # the way auto-merge.yaml warns when BACKLOG_APP_ID is unset.
      - name: Warn when no trace will be published
        if: >-
          env.DAGGER_CLOUD_TOKEN == '' &&
          (github.event_name != 'pull_request' ||
          github.event.pull_request.head.repo.full_name == github.repository)
        run: |
          echo "::warning title=No Dagger Cloud trace::DAGGER_CLOUD_TOKEN is unset, so \
          this run publishes no trace and every failure below has to be read out of this \
          log by eye. See the comment on this job for where an operator gets one."

      # No arguments, because the module's source defaults to the repository
      # root and its lint configuration defaults to the .golangci.yml committed
      # beside it. This is character-for-character what a contributor runs
      # before pushing, so there is no arrangement of local commands that passes
      # while CI fails.
      - name: Run the pipeline
        run: dagger call ci

      # docs/plugin/SPEC.md's determinism requirement, checked rather than
      # asserted (#120). It builds the four binaries for each platform the image
      # ships on, generates the worked example twice under deliberately
      # different surroundings — different absolute paths, working directory,
      # temporary directory, user, hostname, locale and time zone — and requires
      # the two trees to be byte-identical, and identical to what is committed.
      #
      # It is a separate call rather than part of `ci` because `ci` is the
      # Z5Labs standard pipeline and this is avroc's own, not a stage every
      # Z5Labs repository has. Determinism is the kind of property that holds
      # until nobody is looking, and generated code that changes when nothing
      # changed makes the committed output worthless.
      - name: Check that regeneration is byte-identical
        run: dagger call regeneration

      # docs/container/SPEC.md's compatibility guarantees table, executed
      # rather than read (#126). It builds the published base image for each
      # platform it ships on and checks every promise that document makes about
      # it: the plugin directory and its place on PATH, the entrypoint, the
      # empty Cmd, the working directory, the pinned non-root UID, the absence
      # of a shell — as an exact listing of every path in the image — the
      # FileDescriptorSet's path, and the image actually generating the worked
      # example through its own entrypoint, as itself and as an overridden UID.
      #
      # It runs on every pull request rather than only at release time because
      # each of those promises is depended on from a repository this project
      # cannot see, and breaks without breaking anything here: an image whose
      # PATH lost /usr/local/bin runs avroc perfectly and fails at the point
      # where somebody else's generator is not found. Nothing is pushed, so
      # this job still needs no `packages: write`; release.yaml is what
      # publishes.
      - name: Check the base image against its contract
        run: dagger call image-contract

      # The other half of that contract: the images avroc's own three generators
      # ship in, each built FROM the base with nothing but a COPY (#127). It
      # checks that a generator image inherits the base's entrypoint, Cmd, user,
      # working directory and PATH unchanged, that its filesystem is the base's
      # plus exactly one executable at the promised path — which is what makes
      # "only COPY ran" an assertion rather than a claim — that each image
      # generates with its own generator through the inherited entrypoint and no
      # further configuration, and that an image combining all three reproduces
      # the committed worked example byte for byte.
      #
      # It is a separate step from the one above because it is a separate claim:
      # the base can keep every promise it makes while the extension mechanism
      # built on it has stopped working, and this is the step that says which of
      # the two broke.
      - name: Check the generator images against the contract
        run: dagger call generator-image-contract

      # docs/container/SPEC.md's "No certificate authorities", executed (#198).
      # It posts an OTLP export from the published image to a real collector
      # over `http://` and over `https://`, and requires the plaintext one to
      # arrive, the TLS one to fail naming x509, and the TLS one to arrive the
      # moment a CA bundle is supplied at the conventional path.
      #
      # It is a step of its own because the listing the step above asserts can
      # only say that no bundle is in the image; what an operator is being asked
      # to act on is the consequence, which is that an `https://` OTLP endpoint
      # configured on this image never delivers a span. That failure arrives at
      # export time, in somebody's deployment, on a run that otherwise looks
      # like it worked — and it is invisible to every other check here, all of
      # which either configure no telemetry or make no claim about where a span
      # ends up.
      - name: Check TLS egress from the images
        run: dagger call tls-egress

      # One connected trace from this pipeline, through avroc, into every
      # generator it forks (#199). It runs example/'s generation through the
      # published image with its spans going to a real OpenTelemetry Collector
      # backed by Tempo, then queries Tempo and asserts the trace's *structure*:
      # one trace id across avroc's spans and every generator's, each generator's
      # invocation span parented to the avroc span that forked it, and avroc's
      # own root parented to the Dagger exec's span.
      #
      # It is a step of its own because propagation is the one property here that
      # breaks with nothing else breaking. A generator whose span context was
      # dropped generates correct output and exits zero, so every step above
      # stays green and the only symptom is an orphan span in a backend nobody is
      # watching during CI. The unit tests either side of the fork each stop at
      # the process boundary they are testing; this is the only check that sees
      # the spans meet again in a backend.
      #
      # It carries its own negative control — the same assertions against a build
      # with TRACEPARENT propagation removed, which they must reject — so the
      # failure path this step exists for is exercised on every run rather than
      # only on the day it is needed.
      - name: Check that the trace is one connected trace
        run: dagger call trace-propagation

      # docs/container/SPEC.md's worked example, built and run rather than read
      # (#129). It extracts the multi-stage Dockerfile that document gives a
      # stranger, compiles the third-party generator in it with the build stage
      # exactly as committed, copies the result into the plugin directory of the
      # base image this pipeline just built, and generates with it through the
      # inherited entrypoint.
      #
      # It is a step here because the worked example is the one part of this
      # repository nothing else reads: the Go build, the tests and the image
      # checks would all pass on a document whose Dockerfile stopped building
      # releases ago. That example is the first thing an adopter tries, so one
      # that has rotted is worse than none at all — and this is the step that
      # says so on the pull request that broke it rather than in somebody else's
      # terminal.
      - name: Build and run the worked example
        run: dagger call worked-example

      # The companion Dagger module, driven over the example project (#130). It
      # composes avroc's three generators into the base image this pipeline just
      # built — both ways the module offers, out of the generator images and out
      # of the built executables — generates example/ through it, and requires
      # the committed tree back byte for byte.
      #
      # The images are passed in rather than pulled because the module's
      # defaults name a *released* image: a check on the last release would keep
      # passing through a pull request that broke this one. That is the whole
      # reason the module takes an --image argument, and this step is what uses
      # it.
      #
      # It is a step of its own because the module is the one path an adopter
      # takes without learning the container contract underneath, so a
      # convenience that has quietly stopped composing fails for somebody with
      # no reason to know where to look. Nothing else here reads it: the image
      # checks above drive the images directly and would not notice.
      - name: Check the companion Dagger module
        run: dagger call companion-module

      # docs/container/SPEC.md's tag table, executed (#128). It runs the same
      # derivation a release runs — refs at HEAD in, published tags out — over
      # the cases that matter: a release, a patch release, a prerelease that
      # must move none of the tags a derived Dockerfile pins, and the several
      # shapes of ref that are not a release at all.
      #
      # It is here rather than at release time because a release is the one run
      # where finding out is too late: a published version tag is never
      # repointed, so a tag that moved when it should not have is a mistake this
      # project's own contract forbids it from correcting.
      - name: Check the tag scheme
        run: dagger call tag-scheme

      # The one artifact this repository builds, built on every pull request
      # rather than only at release time. Its contents are asserted by the tests
      # `dagger call ci` just ran — that the set covers proto/, resolves on its
      # own and decodes a descriptor leaving nothing undescribed — and what this
      # step adds is that the artifact still *builds*, so a release is never the
      # first time anybody finds out that it does not. It is exported to the
      # runner's temporary directory and discarded; release.yaml is what
      # publishes it.
      - name: Build the IR FileDescriptorSet
        run: dagger call ir-descriptor-set export --path "$RUNNER_TEMP/ir.binpb"
